# Copy to .env.local. `npx convex dev` writes this automatically on first run.
# This is the public deployment URL the browser uses to reach Convex.
VITE_CONVEX_URL=https://your-deployment-name.convex.cloud
